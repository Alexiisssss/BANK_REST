spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;LOCK_TIMEOUT=5000
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
  liquibase:
    enabled: false

app:
  jwt:
    secret: test-secret-please-change-me-256bits-min
    expiration-minutes: 60
  crypto:
    aes-key-b64:

logging:
  level:
    root: WARN
